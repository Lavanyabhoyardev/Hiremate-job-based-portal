FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
# Custom Nginx config can be added here if needed for single-page apps
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
